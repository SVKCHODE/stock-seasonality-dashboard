# Stock Seasonality Dashboard

A Next.js dashboard for exploring historical monthly stock seasonality.

## Current status

The first UI prototype is committed. It uses clearly labeled illustrative data while the market-data connector is being implemented. API secrets must never be committed to this repository.

## Run locally

```bash
npm install
npm run dev
```
